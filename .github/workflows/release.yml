name: release

# Managed by maelys-release untagged (0.14.1\n). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose Homebrew publication is replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    if: github.event_name == 'push'
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@55a9ac4ca9359fafdf70bddab9669dbc05d8d35a # untagged
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: agent-cli-spec
