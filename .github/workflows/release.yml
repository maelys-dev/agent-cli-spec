name: release

# Managed by maelys-release v0.51.0 (0.51.0). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose release and Homebrew publication are replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@bcb2542da9e10b1a7c7222aec268a85243aa18d5 # v0.51.0
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: agent-cli-spec
      tag: ${{ inputs.tag || github.ref_name }}
