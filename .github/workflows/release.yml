name: release

# Managed by maelys-release untagged (0.51.0). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose release and Homebrew publication are replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@e81d146142512fcef71fcede706c7b9e7d265f49 # untagged
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: agent-cli-spec
      tag: ${{ inputs.tag || github.ref_name }}
