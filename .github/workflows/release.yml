name: release

# Managed by maelys-release untagged (0.15.3). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose release and Homebrew publication are replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@403f6bc891d2c4e0aacf5124863c94ae92de9bc9 # untagged
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: agent-cli-spec
      tag: ${{ inputs.tag || github.ref_name }}
